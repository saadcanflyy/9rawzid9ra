# 9rawZid9ra — Design system brand book

> **Missing.** The redesign kit's file listing describes this file as "the full brand book + page blueprints + every component guideline," but the content that was copied into `src/design-system/` never included it — the other four files (`tokens.css`, `ui.js`, `qz.css`, `theme.js`) were each shifted one slot from where they belonged, and this file held a fifth copy of `theme.js` instead of the real brand book. The original zip is no longer available to re-extract from.
>
> Nothing in Prompt 0 strictly depends on this file (it only needs `tokens.css`, `ui.js`, `qz.css`, `theme.js`), so setup can proceed without it. If later prompts want the brand-book guidance, re-export it from wherever the kit was generated, or ask for it to be reconstructed from the DESIGN RULES block in `CLAUDE.md`.
